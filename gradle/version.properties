# Single source of truth for the app version.
# Bump with:  scripts/bump-version.sh <major.minor.patch>
# VERSION_CODE is derived as  major*10000 + minor*100 + patch  and must only ever increase.
VERSION_NAME=0.9.0
VERSION_CODE=900
